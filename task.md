# task.md — 実装計画

原則: **1タスク=1セッションで完結し、毎回「動く状態」で終わる**。完了ごとにコミット。
（設計の理由は `開発メモ.md` を参照）

## Phase 0: 準備（データが全ての土台）
- [x] **P0-1** Git初期化・GitHubリポジトリ作成（S）✓完了 https://github.com/ryoo-ma638/NogiPhotoManager46
      旧Xcode一式も「iOS版アーカイブ」としてコミットしてから `legacy-ios/` へ。`.gitignore` 整備。
      ✅ git logに初回コミット、GitHubで閲覧可
- [ ] **P0-2** カタログJSONスキーマ確定＋変換・検証スクリプト（M）
      原文リスト(✓/◦)→**①公開カタログJSON(所有を含めない)＋②非公開の所有ファイル**に分割変換（**注記=欠けルール**、開発メモ§3）。②はgitignoreしP1-5でインポート。検証: ID重複/枠数/件数。**矛盾は「要確認レポート」に出力**。
      ✅ 5テンプレ全種のサンプルが検証を通過
- [ ] **P0-3** 全カタログのJSON化 `yumiki_nao.json`（L・バインダー単位で分割実施）
      2020&2021 → 2022&2023 → 2024&2025 → 2026&2027 → 封入 の5チャンク。各チャンクごとに検証+コミット（**公開するのはカタログのみ。所有ファイルはコミットしない**）。
      ✅ 約390セット・約1300枠、✓集計が原文と一致、要確認レポートを本人が裁定済み
- [x] **P0-4** Vite+React+TS+Tailwind+PWA の雛形作成、Vercelデプロイ（S）✓完了 https://nogi-photo-manager46.vercel.app
      ✅ iPhoneでURLを開き、ホーム画面に追加して起動できる

## Phase 1: 所有トラッカーMVP 🎯「使える道具」の完成
- [ ] **P1-1** カタログ読込＋型定義＋IndexedDB所有ストア（Dexie）（M）
      catalog JSONのfetch/parse、`{photoId:{owned,ownedDate}}`。初回seed（ownedフィールド反映・冪等）。Vitestでテスト。
      ✅ テスト通過、seed後の所有数がJSON集計と一致
- [ ] **P1-2** 画面: バインダー一覧（全体コンプ率+進捗バー）→ 年一覧（M）
      封入バインダーは年層スキップ。ヘッダーにメンバー名（切替メニューの器だけ）。
      ✅ 実データの進捗%が表示される
- [ ] **P1-3** 画面: セット一覧（M）
      カタログ順(sortIndex)、ページ区切り表示、セグメント「すべて/未コンプ/コンプ」。single1は行から直接トグル。
      ✅ 原文と同じ順序・件数で並ぶ
- [ ] **P1-4** 画面: セット詳細=ポーズグリッド＋所有トグル（M）**←MVPの核**
      カードタップでトグル→即保存。右上チェックは独立領域。下部「すべて所有」（確認ダイアログ付き）。コンプ表示。
      ✅ トグル→リロード後も保持。一覧の進捗が連動
- [ ] **P1-5** エクスポート/インポート（S）**←生命線。P1-6より先に必ず**
      所有データをJSONダウンロード/読込復元。設定画面に配置。
      ✅ export→全消去→importで完全復元
- [ ] **P1-6** iPhone実機で運用開始（S）
      ホーム画面追加・オフライン起動確認・数十件○×を付けて使用感メモ。`navigator.storage.persist()`。
      ✅ 翌日も○×が残っている
- [ ] **P1-7** 検索＋統計タブ（M）
      全セット横断の名前検索（表記ゆれは正規化比較）。統計: 総所有/バインダー別/年別/レアリティ別。
      ✅ 部分一致でヒット、統計が手計算と一致
- [ ] **P1-8** 手動セット追加＋ポーズ枠編集（M）
      テンプレ選択で追加(`user-` ID)、枠の増減・改名。カタログ更新（push→デプロイ）で消えないこと。
      ✅ 追加・編集がカタログ更新後も残る

**P1完了基準**: iPhoneで 390セット閲覧・○×・進捗・検索・バックアップが全部動く → 実運用開始

## Phase 1.5: 画像の手動添付
- [ ] P15-1 写真枠に画像添付（`<input type="file">`、保存前に長辺1024pxへ縮小、IndexedDBにBlob）（M）
- [ ] P15-2 サムネ表示・拡大ビューア・差し替え/削除（削除しても○×は不変）（M)
- [ ] P15-3 画像30枚以上でのスクロール性能確認（S）

## Phase 2: カメラ取込
- [ ] P2-1 カメラ起動（`capture` / getUserMedia）→撮影→手動crop（cropper系ライブラリ）→枠に割当て（L）
      ※自動一括切り抜きはやらない（開発メモ§7）
- [ ] P2-2 セット単位の連続取込フロー（M）

## Phase 3: 自動識別（スパイクのみ・go/no-go判断）
- [ ] P3-1 画像類似での候補サジェストを使い捨てコードで検証→実用ラインに届かなければ潔く closed（M）

## 最初の2セッション
1. **P0-1 + P0-4**（土台と雛形。1〜2時間で「iPhoneで開けるアプリの箱」ができる）
2. **P0-2**（変換ルールの確定が全体の品質を決めるので独立セッションで）
