# legacy-ios（アーカイブ）

SwiftUI + SwiftData で試作していたiOS版。以下の理由で開発を停止し、Web版（リポジトリ直下）へ移行した。

- 無料Apple IDでの実機インストールは署名が7日で失効し、日常使いに向かない
- 画像の自動切り抜き（Vision矩形検出）が不安定で、まず所有管理を優先する方針に変更

参照用に残しているだけで、ビルド・利用はしない。
